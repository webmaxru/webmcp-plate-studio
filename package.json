{
  "name": "webmcp-plate-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A deterministic human-agent microplate design studio powered by imperative WebMCP tools.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check src/domain.js && node --check src/webmcp.js && node --check src/app.js && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
